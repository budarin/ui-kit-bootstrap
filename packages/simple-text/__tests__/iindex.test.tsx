// import * as React from 'react';
// import { shallow } from 'enzyme';
// import Button from './index';

test('CheckboxWithLabel changes the text after click', () => {
    // const btn = shallow(<Button text="Off" />);
    // Interaction demo
    // expect(btn.text()).toEqual('Off');
    // btn.find('input').simulate('change');
    // expect(btn.text()).toEqual('On');
    // Snapshot demo
    // expect(btn).toMatchSnapshot();

    expect(0).toBe(0);
});
